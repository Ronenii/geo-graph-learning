# Unit 4 — Dynamic Navigation

**Capability after this unit:** Model edge weights as w(t) when
warranted (often derived from U3's empirical speeds). Pick road vs.
transit abstraction by data and question. Run Dijkstra / A* / TDSP
(road) or RAPTOR / CSA (transit). Compute and interrogate decision-
space objects (isochrones, alternatives, multimodal Cost of Anarchy).

**Status:** _coming soon._ When the lesson publishes, this README will
be replaced with overview text and links to `theory.md`, `demo.ipynb`,
`practice-task.md`, `homework.md`, `datasets.md`, `further-reading.md`
(including a **RECENT** sub-section on learned A* heuristics and
deep-RL transit network design).

> **Numbering note.** Dynamic Navigation is **Unit 4** in our teach
> order. The original syllabus PDF labels it Unit 3. Every public-side
> artifact uses the teach-order numbering.

Until then:

- The shared rubric is at [`../rubric.md`](../rubric.md).
- The decision-log template is at
  [`../decision-log-template.md`](../decision-log-template.md).
- Your work goes in [`student-work/`](./student-work/).
